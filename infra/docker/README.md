# Docker Notes

Local development uses Postgres, Redis, and MinIO. Production should point the same S3-compatible storage configuration at Cloudflare R2.

MinIO console:

- URL: http://localhost:9001
- User: `minioadmin`
- Password: `minioadmin`
